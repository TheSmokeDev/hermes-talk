__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.ruff_cache/
dist/
# The dashboard bundle is SOURCE, not a build artifact — it is hand-authored
# and the host serves it straight from the installed plugin, so an operator
# who clones this repo must get it. Same reason the in-tree kanban and
# achievements plugins commit theirs.
!dashboard/dist/
build/
*.egg-info/
.env
node_modules/
.codex/
uv.lock
